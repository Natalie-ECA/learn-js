import { describe, it, expect } from "vitest";
// write some code that passes the tests

export function returnElementAtPosition(array, position) {

}

export function removeLastElement() {

}


// write some more tests to practice using different array methods e.g. length, splice, join, map, filter, reduce, etc.